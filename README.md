# ДЗ №6: Индексы (Hash-Index и B+-Tree)

Вам будет необходимо реализовать два типа индексов в вашей СУБД: **Hash-Index** для быстрого поиска по равенству и **B+-Tree Index** для поддержки range-запросов. Затем интегрировать их в систему через Executor'ы.

---

## 📋 Структура домашнего задания

Домашнее задание разделено на **четыре последовательные части**:

### Часть 1. Hash-Index Реализация
Реализовать `HashIndexImpl` с поддержкой линейного хеширования

### Часть 2. Hash-Index Executor
Реализовать `HashIndexScanExecutor` для интеграции в цепочку исполнения

### Часть 3. B+-Tree Index Реализация
Реализовать `BPlusTreeIndexImpl` с поддержкой range-запросов

### Часть 4. B+-Tree Executor
Реализовать `BTreeIndexScanExecutor` для интеграции в цепочку исполнения

---

## Часть 1. Hash-Index Реализация

### Входные данные

Базовые интерфейсы уже определены:

- **`Index`** — базовый интерфейс для всех индексов
  - `void insert(Comparable key, TID tid)` — вставка записи (ключ → адрес строки)
  - `String getName()` — имя индекса
  - `IndexType getType()` — тип индекса (HASH или BTREE)
  - `String getColumnName()` — имя столбца, на котором построен индекс
  - **Примечание:** метод `delete()` НЕ требуется к реализации в этом задании

- **`HashIndex`** — специализированный интерфейс для хеш-индекса
  - `List<TID> search(Comparable key)` — поиск по равенству
  - `List<TID> scanAll()` — полное сканирование индекса (для отладки)
  - `int getNumBuckets()` — количество бакетов
  - `long getRecordCount()` — количество записей
  - `int getMaxBucket()` — максимальный номер бакета

- **`TID`** (Tuple IDentifier) — физический адрес строки
  - `int pageId` — номер страницы
  - `short slotId` — номер слота на странице

- **`IndexType`** — enum с типами индексов
  - `HASH` — хеш-индекс
  - `BTREE` — B+-tree индекс

### Выходные данные

Вам нужно реализовать **`HashIndexImpl`** класс, который:

1. **Инициализирует индекс** при создании (конструктор):
   - Начинает с 16 бакетов (степень 2)
   - Инициализирует маски (lowmask, highmask)
   - Устанавливает splitPointer = 0

2. **Реализует метод `insert(Comparable key, TID tid)`**:
   - Вычисляет хеш ключа
   - Определяет бакет по маскам (linear hashing)
   - Вставляет запись в бакет (или overflow-страницу)
   - При переполнении инициирует split бакета

3. **Реализует метод `search(Comparable key)`**:
   - Вычисляет хеш ключа
   - Находит правильный бакет по маскам
   - Возвращает список всех TID'ов с этим ключом
   - Обрабатывает коллизии и overflow-страницы

4. **Реализует метод `scanAll()`**:
   - Возвращает все TID'ы в индексе
   - Используется для проверки целостности и отладки

5. **Вспомогательные методы**:
   - `int hashFunction(Comparable key)` — функция хеширования для разных типов
   - `int computeBucket(int hash)` — вычисление номера бакета с поддержкой linear hashing
   - `void performSplit()` — раскол бакета при переполнении

### Детали реализации

**Линейное хеширование (Linear Hashing):**

Индекс использует **динамическое расширение** вместо простого rehashing всей таблицы:

1. При переполнении добавляется **ровно один новый бакет**
2. Раскалывается **один старый бакет** в два (по битам хеша)
3. Обновляются маски (lowmask, highmask)

**Формула вычисления бакета:**
```
bucket = hash & highmask
if (bucket > maxBucket):
    bucket = hash & lowmask
```

**Пример:**
- Начальное состояние: numBuckets = 16, lowmask = 0xF (4 бита), highmask = 0xF
- После первого split: numBuckets = 17, lowmask = 0xF, highmask = 0x1F (5 бит)
- После 16-го split: numBuckets = 32, lowmask = 0x1F, highmask = 0x1F

**Обработка переполнений:**

Если бакет переполнен, создаются **overflow-страницы**, связанные в цепочку через `nextOverflowPageId`.

### Сигнатуры методов в `HashIndexImpl` представлены в классе `HashIndexImpl`


### Метрики и характеристики

- **Сложность вставки:** O(1) в среднем
- **Сложность поиска:** O(1) в среднем
- **Поддержка range-запросов:** ❌ НЕ поддерживается
- **Динамическое расширение:** ✅ Линейное хеширование
- **Работа на диске:** ✅ PageFileManager + overflow-страницы

### Что должно быть реализовано

- Конструктор с инициализацией
- `insert(key, tid)` с поддержкой overflow и split'ов
- `search(key)` с обработкой коллизий
- `scanAll()` для полного сканирования
- Вспомогательные методы хеширования и вычисления бакета

**Важно:** Метод `delete()` НЕ требуется к реализации в рамках этого задания.

---

## Часть 2. Hash-Index Executor

### Входные данные

Физический план выполнения запроса указывает на использование Hash-Index:
- Имя индекса
- Таблица, из которой нужно читать строки
- Значение ключа для поиска (например, `id = 42`)

### Выходные данные

Executor, который реализует интерфейс `Executor`:
```java
public interface Executor {
    void open();       // Инициализация
    Object next();     // Получить следующую строку (или null если конец)
    void close();      // Закрытие ресурсов
}
```

### Структура полей в `HashIndexScanExecutor`:

```java
public class HashIndexScanExecutor implements Executor {
    private final HashIndex index;           // Индекс для поиска
    private final Comparable<?> searchKey;      // Ключ для поиска
    private final Table table;               // Таблица для чтения строк
    private Iterator<TID> tidIterator;       // Итератор по результатам
    private boolean isOpen;
    
    ...
}
```

### Логика работы

1. **`open()`** — выполняет поиск в индексе
   - Вызывает `index.search(searchKey)`
   - Получает список TID'ов (адресов строк)
   - Создаёт итератор по этому списку

2. **`next()`** — выдаёт результаты одну за другой
   - Берёт следующий TID из итератора
   - Читает строку из таблицы (используя TID)
   - Возвращает строку или null если результатов нет

3. **`close()`** — завершает работу
   - Отмечает, что executor закрыт

### Интеграция в ExecutorFactory

При создании executor'а для `PhysicalIndexScanNode` типа HASH:

```java
public Executor createExecutor(PhysicalPlanNode node) {
    if (node instanceof PhysicalIndexScanNode) {
        PhysicalIndexScanNode indexNode = (PhysicalIndexScanNode) node;
        
        if (indexNode.getIndexType() == IndexType.HASH) {
            HashIndex index = (HashIndex) catalogManager.getIndex(
                indexNode.getIndexName()
            );
            Table table = catalogManager.getTable(
                indexNode.getTableName()
            );
            Comparable searchKey = indexNode.getValue();
            
            return new HashIndexScanExecutor(index, searchKey, table);
        }
    }
    // ... обработка других типов
}
```

### Что должно быть реализовано

- Конструктор `HashIndexScanExecutor(index, key, table)`
- Метод `open()` выполняет поиск в индексе
- Метод `next()` возвращает строки одну за одной
- Метод `close()` закрывает executor
- Правильная обработка null (когда результатов нет)

---

## Часть 3. B+-Tree Index Реализация

### Входные данные

Базовый интерфейс `BPlusTreeIndex` с методами:
- `void insert(Comparable key, TID tid)` — вставка
- `List<TID> search(Comparable key)` — поиск по равенству
- `List<TID> rangeSearch(Comparable from, Comparable to)` — range-запрос
- `List<TID> searchGreaterThan(Comparable value)` — поиск > значения
- `List<TID> searchLessThan(Comparable value)` — поиск < значения
- `List<TID> scanAll()` — полное сканирование
- `int getHeight()` — высота дерева
- `int getOrder()` — порядок дерева (M)

### Выходные данные

Реализовать **`BPlusTreeIndexImpl`** класс, который:

1. **Инициализирует дерево** (конструктор):
   - Создаёт корневой лист
   - Устанавливает высоту = 1
   - Сохраняет порядок (order/M) для вычисления capacity

2. **Реализует метод `insert(Comparable key, TID tid)`**:
   - Находит листовой узел для вставки
   - Вставляет запись в отсортированном порядке
   - При переполнении листа — раскалывает его
   - Рекурсивно обрабатывает split'ы внутренних узлов
   - Создаёт новый корень при раскале текущего корня

3. **Реализует метод `search(Comparable key)`**:
   - Спускается от корня к листу
   - Находит все записи с этим ключом
   - Возвращает список TID'ов

4. **Реализует метод `rangeSearch(Comparable from, Comparable to)`**
   - **САМЫЙ ВАЖНЫЙ метод для B+-Tree!**
   - Спускается от корня к первому листу с ключом >= from
   - Обходит листья слева направо по sibling-ссылкам
   - Собирает все TID'ы в диапазоне [from, to]
   - Сложность: O(log n + k), где k = количество результатов

5. **Реализует методы `searchGreaterThan` и `searchLessThan`**:
   - Используют sibling-ссылки для efficient поиска
   - Поддерживают инклюзивные и эксклюзивные границы

6. **Реализует метод `scanAll()`**:
   - Обходит все листья слева направо
   - Возвращает все TID'ы в отсортированном порядке

### Детали реализации

**B+-Tree отличается от обычного B-Tree:**

1. **Данные только в листьях**
   - Внутренние узлы хранят только ключи для навигации
   - Листья содержат (ключ → TID) пары

2. **Листья связаны двусвязным списком**
   - `leftSiblingPageId` и `rightSiblingPageId` в каждом листе
   - Позволяет эффективный обход для range-запросов

3. **Гарантированная сбалансированность**
   - Все листья находятся на одном уровне
   - После split'ов дерево остаётся сбалансированным

**Порядок дерева (M):**
- Обычно 250-400 для 8KB страниц
- `numKeys` в узле: от M-1 (минимум, кроме корня) до 2M-1 (переполнение)

### Сигнатуры методов в `BPlusTreeIndexImpl` представлены в классе `BPlusTreeIndexImpl`

### Метрики и характеристики

- **Сложность вставки:** O(log n)
- **Сложность поиска:** O(log n)
- **Сложность range search:** O(log n + k), где k = результаты
- **Поддержка range-запросов:** ✅ Полная поддержка
- **Поддержка сортировки:** ✅ Дерево упорядочено
- **Гарантированная сбалансированность:** ✅ Все листья на одном уровне

### Что должно быть реализовано

- Конструктор с инициализацией корневого листа
- `insert(key, tid)` с поддержкой split'ов
- `search(key)` — поиск по равенству
- `rangeSearch(from, to)` — **KEY FEATURE** для range-запросов
- `searchGreaterThan(value)` — поиск > значения
- `searchLessThan(value)` — поиск < значения
- `scanAll()` — полное сканирование листьев
- Sibling-ссылки для эффективного обхода

**Важно:** Метод `delete()` НЕ требуется к реализации в рамках этого задания.

---

## Часть 4. B+-Tree Executor

### Входные данные

Физический план выполнения запроса с range-условием:
- Имя индекса
- Таблица, из которой нужно читать строки
- Границы диапазона (from, to) или условие (>, <, >=, <=)

### Выходные данные

Executor, реализующий интерфейс `Executor` для B+-Tree поиска

### Структура полей в `BTreeIndexScanExecutor`

```java
public class BTreeIndexScanExecutor implements Executor {
    private final BPlusTreeIndex index;       // B+-Tree индекс
    private final Comparable rangeFrom;       // Нижняя граница (может быть null)
    private final Comparable rangeTo;         // Верхняя граница (может быть null)
    private final Table table;                // Таблица для чтения строк
    private Iterator<TID> tidIterator;        // Итератор по результатам
    private boolean isOpen;
    
    ...
}
```

### Логика работы

Аналогично `HashIndexScanExecutor`, но использует `rangeSearch()`:

1. **`open()`** — выполняет range-поиск
   - Вызывает `index.rangeSearch(from, to)` (сложность O(log n + k))
   - Получает список TID'ов в порядке сортировки

2. **`next()`** — выдаёт результаты в порядке сортировки
   - Возвращает строки одну за одной
   - Результаты уже отсортированы (благодаря B+-Tree структуре)

3. **`close()`** — завершает работу

### Интеграция в ExecutorFactory

```java
public Executor createExecutor(PhysicalPlanNode node) {
    if (node instanceof PhysicalIndexScanNode) {
        PhysicalIndexScanNode indexNode = (PhysicalIndexScanNode) node;
        
        if (indexNode.getIndexType() == IndexType.BTREE) {
            BPlusTreeIndex index = (BPlusTreeIndex) catalogManager.getIndex(
                indexNode.getIndexName()
            );
            Table table = catalogManager.getTable(indexNode.getTableName());
            
            // Для range-запроса
            if (indexNode.hasRange()) {
                return new BTreeIndexScanExecutor(index, indexNode.getRangeFrom(), indexNode.getRangeTo(), table);
            } else { // Для простого поиска по равенству
                return new BTreeIndexScanExecutor(index, indexNode.getValue(), table);
            }
        }
    }
    // ... обработка других типов
}
```

### Что должно быть реализовано

- Два конструктора (для range и для одиночного поиска)
- Метод `open()` выполняет range-поиск
- Метод `next()` возвращает строки в отсортированном порядке
- Метод `close()` закрывает executor

---

## Тесты

Напишите всесторонние тесты для каждой части:
- отдельно тесты на hash-index (позитивные + негативные)
- отдельно тесты на hash-index executor (позитивные + негативные)
- отдельно тесты на bplus-tree-index (позитивные + негативные)
- отдельно тесты на bplus-tree-index executor (позитивные + негативные)


---

## Демонстрация функциональности

Напишите простой класс для демонстрации:

```java
public class IndexDemo {
    public static void main(String[] args) {
        // 1. Создать индексы
        HashIndex hashIndex = new HashIndexImpl("idx_users_id", "id", ...);
        BPlusTreeIndex btreeIndex = new BPlusTreeIndexImpl("idx_orders_date", "date", ...);
        
        // 2. Вставить данные
        for (int i = 0; i < 1000; i++) {
            hashIndex.insert(i, new TID(0, i));
            btreeIndex.insert(i * 10, new TID(1, i));
        }
        
        // 3. Демонстрировать поиск
        System.out.println("Hash Index search(42): " + hashIndex.search(42));
        System.out.println("B+-Tree range [100, 500]: " + 
            btreeIndex.rangeSearch(100, 500));
        
        // 4. Демонстрировать executor'ы
        Executor executor = new HashIndexScanExecutor(hashIndex, 42, table);
        executor.open();
        Object result;
        while ((result = executor.next()) != null) {
            System.out.println("Row: " + result);
        }
        executor.close();
    }
}
```
